/**
 * GitHub page hook (optional). Does not access OAuth tokens or storage.
 * Future: bridge UI into github.com tabs.
 */
(function contentScriptIife() {
  /* reserved for future GitHub DOM integrations */
})();
